rootProject:
  name: hackathon

spring:
  config:
    import:
      - classpath:application-secret.yml
      - classpath:application-database.yml
      - classpath:application-weather.yml

springdoc:
  swagger-ui:
    path: /swagger